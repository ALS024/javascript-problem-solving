
let strInput = "This is a coding test, I like coding because it is fun"
let strOutput = strInput.reverse();

// for (let char of strInput) {
//    strOutput = char + strOutput;
// }

function removeDuplicatesToRight<T>(array: T[]) {
   return array.filter((value, index) => array.indexOf(value) === index);
}

var arrOutput = new Array[strOutput];
removeDuplicatesToRight(arrOutput);


